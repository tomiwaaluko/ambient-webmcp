# Ambient

Read **`AGENTS.md`** at the repo root. It is the entry point for every agent on this project regardless of harness, and it routes you to the conventions, the work graph, and the plan.

Do not skip it — it carries the non-negotiable constraints (no build step, no dependencies, never overclaim) that bind regardless of what an individual ticket says.
